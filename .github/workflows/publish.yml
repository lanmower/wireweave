name: publish

on:
  push:
    branches: [main]

jobs:
  publish:
    if: "!contains(github.event.head_commit.message, '[skip-publish]') && github.event.head_commit.author.name != 'github-actions[bot]'"
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'

      - name: install
        run: npm install --no-package-lock

      - name: test
        run: npm test

      - name: configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: bump version
        run: |
          PUBLISHED=$(npm view wireweave version 2>/dev/null || echo "0.0.0")
          LOCAL=$(node -p "require('./package.json').version")
          BASE=$(node -e "
            const a='$PUBLISHED'.split('.').map(Number), b='$LOCAL'.split('.').map(Number);
            const gt = a[0]!==b[0]?a[0]>b[0]:a[1]!==b[1]?a[1]>b[1]:a[2]>b[2];
            console.log(gt?'$PUBLISHED':'$LOCAL');
          ")
          npm version "$BASE" --no-git-tag-version --allow-same-version
          npm version patch --no-git-tag-version

      - name: publish
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: commit + tag
        run: |
          VERSION=$(node -p "require('./package.json').version")
          git add package.json
          git commit -m "chore: release v$VERSION [skip-publish]"
          git tag "v$VERSION"
          git push origin main --follow-tags
