---
key: mem-2d5f434b89f3b94f-1033
ns: default
created: 1787652852040
updated: 1787652852040
---

security: a HiddenSpawn-class malware payload (obfuscated _0x-hex identifiers appended to a legitimate file as one long line) can be smuggled directly INTO a real git commit alongside a genuine, unrelated-looking fix -- not just dropped as an untracked file. Confirmed on C:\dev\wireweave: flatspace.config.mjs's payload was committed in 087de8d (2026-08-11, subject "fix: channels.create() now returns the created channel object"), bundled with a real one-line channels.js fix AND a full-file .gitignore rewrite that silently dropped .env/.env.* from the ignore list (credential-exfiltration setup). Live on public GitHub for two weeks before scan_deps caught it. A first-pass diagnosis (checking git status before the file was ever mutated this session) wrongly concluded it was untracked/resident since May -- git log -p -- <path> is what actually revealed the truth. Lesson: (1) run scan_deps periodically, not only on fresh installs; (2) never trust a commit's stated subject as evidence of its full diff -- always check the complete file list and content, especially any accompanying .gitignore/config change; (3) a scan_deps hit's tracked-status must be re-checked with git log -p after any mutation, not assumed from one earlier git status read.
